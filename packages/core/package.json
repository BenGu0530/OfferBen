{
  "name": "@offerben/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared business logic: profile/job schemas, AI provider abstraction, prompts, matching and generation.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "build": "echo \"(core) source-only package, transpiled by the consumer\""
  },
  "dependencies": {
    "@google/genai": "^2.9.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
