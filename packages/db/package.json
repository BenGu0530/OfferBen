{
  "name": "@offerben/db",
  "version": "0.1.0",
  "private": true,
  "description": "Supabase client wrappers, row types, and schema.sql (Postgres + RLS). Optional cloud persistence.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(db) no lint configured\"",
    "build": "echo \"(db) source-only package, transpiled by the consumer\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
